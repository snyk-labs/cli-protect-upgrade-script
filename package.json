{
  "name": "@snyk/cli-protect-upgrade",
  "bin": {
    "@snyk/cli-protect-upgrade": "bin/cli-protect-upgrade"
  },
  "files": [
    "bin",
    "index.js"
  ],
  "description": "Upgrade `snyk protect` into the new `@snyk/protect`",
  "version": "1.0.0",
  "author": "snyk.io",
  "license": "Apache-2.0",
  "scripts": {
    "test": "jest",
    "format": "prettier --write '**/*.{js,ts,json,yaml,yml,md}'",
    "lint:formatting": "prettier --check '**/*.{js,ts,json,yaml,yml,md}'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/snyk-labs/cli-protect-upgrade-script.git"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.12",
    "@types/jest": "^27.0.1",
    "fs-extra": "^10.0.0",
    "jest": "^27.1.0",
    "prettier": "^2.3.2",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.2"
  }
}
